# Пароль для входа в /admin
ADMIN_PASSWORD=semenov2026

# Секрет для JWT-токенов (смените на случайную строку)
JWT_SECRET=change-this-to-random-secret-string

# Порт сервера (по умолчанию 3001)
PORT=3001

# Путь к базе SQLite (по умолчанию data/leads.db)
# DB_PATH=./data/leads.db
