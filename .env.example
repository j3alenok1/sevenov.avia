# Prisma Postgres (Vercel) — npx vercel env pull .env.local
# Для API (pooled):
POSTGRES_URL=
# Для миграций (direct):
POSTGRES_URL_NON_POOLING=
DATABASE_URL=

# Временный пароль админки (/admin)
ADMIN_PASSWORD=admin

# JWT — задайте в Vercel Dashboard
JWT_SECRET=your-jwt-secret-here
