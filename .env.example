# Postgres (Prisma / Vercel) — npx vercel env pull .env.local
POSTGRES_URL=
DATABASE_URL=
PRISMA_DATABASE_URL=

# Временный пароль админки (/admin) — смените в продакшене
ADMIN_PASSWORD=admin

# JWT — задайте в Vercel Dashboard (не коммитьте реальный секрет)
JWT_SECRET=your-jwt-secret-here
